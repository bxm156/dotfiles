{{- if .isWork }}
# Homebrew initialization (work machine only)
# Sourced by all zsh shells (interactive, non-interactive, login, non-login)
if [[ -z "$HOMEBREW_PREFIX" ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi
{{- end }}
